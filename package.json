{
  "name": "dominion-sim-api",
  "version": "0.0.1",
  "description": "Collaborative project",
  "author": "mpaulweeks",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "script": "npm run build && node dist/script.js",
    "server": "node dist/server.js",
    "watch": "nodemon --exec 'npm run build && npm run server'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.1",
    "nodemon": "^3.0.1",
    "typescript": "^5.1.6"
  }
}
